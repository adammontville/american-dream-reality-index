# Scripts — placeholder

Retrieval, indicator processing, and composite computation scripts will live here.

They will be added in Thread 2, after the methodology in `../docs/METHODOLOGY.md` is treated as fixed.

Intended structure (from Thread 2 prompt):

- `fetch_data.py` — retrieval / ingest per indicator
- `process_indicators.py` — normalization and per-indicator processed files
- `compute_index.py` — composite calculation
- `run_all.py` — orchestrator
